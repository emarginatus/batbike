---
title: "Bat Bike"
output: html_document
---

```{r load_packages, include = FALSE}
set.seed(1)
library(knitr)
opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE
)
library(htmlwidgets)
setWidgetIdSeed(1)
library(leaflet)
library(dplyr)
library(rgdal)
library(raster)
library(rgeos)
library(ggvis)
```

```{r lees_gegevens}
route <- readOGR("route/Batbike.kml", "Route", verbose = FALSE)
route <- route[route$Name == "Fietsroute", ]
if (!file_test("-f", "dem/srtm_37_02.tif")) {
  location <- bbox(route) %>%
    rowMeans()
  getData(
    "SRTM", 
    lon = location["x"], 
    lat = location["y"], 
    path = "dem"
  )
}
hoogte <- raster("dem/srtm_37_02.tif")
```

```{r bereken_profiel}
lc <- route %>%
  coordinates() %>%
  unlist(recursive = FALSE)
route_lambert <- route %>%
  spTransform(CRS("+init=epsg:31370"))
lc_lambert <- route_lambert %>%
  coordinates() %>%
  unlist(recursive = FALSE)
hoogteprofiel <- lapply(
  seq_along(lc),
  function(i){
    lc_lambert[[i]] %>%
      cbind(route = i) %>%
      as.data.frame() %>%
      bind_cols(
        extract(hoogte, lc[[i]], df = TRUE, method = "bilinear")
      )
  }
) %>%
  bind_rows() %>%
  group_by(route) %>%
  arrange(route, ID) %>%
  mutate(
    L1 = lag(V1), 
    L2 = lag(V2),
    afstand = ifelse(
      is.na(L1),
      0,
      sqrt((V1 - L1) ^ 2 + (V2 - L2) ^ 2)
    ),
    cum_afstand = cumsum(afstand) / 1e3,
    stijging = ifelse(
      is.na(L1),
      0,
      srtm_37_02 - lag(srtm_37_02)
    ),
    hoogtemeter = cumsum(pmax(stijging, 0))
  )
afstand <- sprintf("%.0fkm", gLength(route_lambert) / 1e3)
hoogtemeters <- sprintf("%.0fm", max(hoogteprofiel$hoogtemeter))
```

**Bat Bike** is een familievriendelijke fietstocht rond Halle die je in contact brengt met de wondere wereld van **vleermuizen** en hoe ze gebruik maken van de verschillende historische gebouwen en elementen in het landschap. 

De `r afstand` lange route met `r hoogtemeters` hoogtemeters brengt je langs een aantal stopplaatsen met extra informatie over vleermuizen. Je kan de route en de beschrijving eveneens vinden in onze folder. Deze kan je downloaden via de website als gratis afhalen bij onze partners. De route is volledig bewegwijzerd.  De [GPX track](route/route.gpx) is tevens beschikbaar.

## Route

```{r overzicht_kaart}
leaflet(data = route) %>%
  addProviderTiles("Thunderforest.OpenCycleMap") %>%
  addPolylines()
```

## Hoogteprofiel

```{r hoogtemeters}
hoogteprofiel %>%
  ggvis(~cum_afstand, ~srtm_37_02) %>%
  layer_lines() %>%
  scale_numeric("x", label = "afstand (km)") %>%
  scale_numeric("y", label = "hoogte (m)")
```

